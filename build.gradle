buildscript {
    repositories {
        mavenCentral()
    }


    dependencies {
        classpath files('auto-plugin/build/libs/auto-plugin.jar') // 依赖本地文件，需要将jar文件放到项目根目录
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}
